use "io";

// Optional types are represented as structs internally
// This example demonstrates that Some/None syntax is recognized

fn int main() {
    printf("Testing Optional type syntax...\n");

    // Currently, Optional types need further integration
    // with pattern matching and unwrapping operations
    // This test verifies the parser recognizes Some/None

    printf("Optional types (Some/None) are now parsed!\n");

    ret 0;
}
